.env
.cache
*.txt
.idea
.DS_Store
venv/
cache/
downloads/
__pycache__/
anony/cookies/*.txt
*.session
*.session-journal
# Local VPS deploy helpers (contain SSH credentials — never commit)
.deploy/
# Local clone of the lily_api repo (has its own git remote — never commit here)
.api-work/
